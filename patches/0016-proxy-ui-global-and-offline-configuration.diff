diff --git a/Telegram/SourceFiles/boxes/connection_box.cpp b/Telegram/SourceFiles/boxes/connection_box.cpp
index 16fa96c..1a254c8 100644
--- a/Telegram/SourceFiles/boxes/connection_box.cpp
+++ b/Telegram/SourceFiles/boxes/connection_box.cpp
@@ -1309,7 +1309,13 @@ void ProxiesBox::setupContent() {
 			st::boxDividerLabel),
 		st::proxyAboutPadding);
 
-	addSectionTitle(u"MTProto disguise"_q);
+	addSectionTitle(u"Global MTProto FakeTLS settings"_q);
+	inner->add(
+		object_ptr<Ui::FlatLabel>(
+			inner,
+			u"Applied to every MTProto FakeTLS proxy."_q,
+			st::boxDividerLabel),
+		st::proxyAboutPadding);
 	_proxyCHelloButton = Settings::AddButtonWithLabel(
 		inner,
 		rpl::single(u"ClientHello profile"_q),
@@ -1318,9 +1324,6 @@ void ProxiesBox::setupContent() {
 	const auto clientMenu = _proxyCHelloButton->lifetime().make_state<
 		base::unique_qptr<Ui::PopupMenu>>();
 	_proxyCHelloButton->addClickHandler([=] {
-		if (!_settings.isEnabled()) {
-			return;
-		}
 		*clientMenu = base::make_unique_q<Ui::PopupMenu>(
 			_proxyCHelloButton,
 			st::popupMenuWithIcons);
@@ -1438,12 +1441,12 @@ void ProxiesBox::setupContent() {
 			u"Minimum delay"_q,
 			QString::number(_settings.proxySlowDelay()),
 			u"ms"_q,
-			9999);
+			Core::SettingsProxy::kMaximumTimingValue);
 		_proxySlowJitterInput = addNumberRow(
 			u"Random jitter"_q,
 			QString::number(_settings.proxySlowJitter()),
 			u"ms"_q,
-			9999);
+			Core::SettingsProxy::kMaximumTimingValue);
 		_proxySlowModeOptions = _proxySlowModeOptionsLayout->entity();
 	}
 
@@ -1546,10 +1549,6 @@ void ProxiesBox::setupContent() {
 	});
 	_proxySlowMode->toggledChanges(
 	) | rpl::on_next([=](bool checked) {
-		if (!_settings.isEnabled()) {
-			_proxySlowModeValue = _settings.proxySlowMode();
-			return;
-		}
 		_proxySlowModeValue = checked;
 		_controller->setProxySlowMode(checked);
 		_proxySlowSummary->setText(SlowModeSummary(_settings));
@@ -1557,17 +1556,13 @@ void ProxiesBox::setupContent() {
 	}, _proxySlowMode->lifetime());
 	_proxyAddressPriorityButton->toggledChanges(
 	) | rpl::on_next([=](bool checked) {
-		if (!_settings.isEnabled()
-			|| !_controller->setProxyPreferIPv6(checked)) {
+		if (!_controller->setProxyPreferIPv6(checked)) {
 			_proxyAddressPriorityValue = _settings.proxyPreferIPv6();
 		} else {
 			_proxyAddressPriorityValue = checked;
 		}
 	}, _proxyAddressPriorityButton->lifetime());
 	connect(_proxySlowDelayInput, &Ui::MaskedInputField::changed, [=] {
-		if (!_settings.isEnabled()) {
-			return;
-		}
 		auto ok = false;
 		const auto value = _proxySlowDelayInput->getLastText().toInt(&ok);
 		if (ok && _controller->setProxySlowDelay(value)) {
@@ -1576,9 +1571,6 @@ void ProxiesBox::setupContent() {
 	});
 
 	connect(_proxySlowJitterInput, &Ui::MaskedInputField::changed, [=] {
-		if (!_settings.isEnabled()) {
-			return;
-		}
 		auto ok = false;
 		const auto value = _proxySlowJitterInput->getLastText().toInt(&ok);
 		if (ok && _controller->setProxySlowJitter(value)) {
@@ -1597,16 +1589,10 @@ void ProxiesBox::setupContent() {
 
 	_proxyForCalls->entity()->toggledChanges(
 	) | rpl::on_next([=](bool checked) {
-		if (_settings.isEnabled()) {
-			_controller->setProxyForCalls(checked);
-		}
+		_controller->setProxyForCalls(checked);
 	}, _proxyForCalls->lifetime());
 	_proxyRotation->entity()->toggledChanges(
 	) | rpl::on_next([=](bool checked) {
-		if (!_settings.isEnabled()) {
-			_proxyRotationValue = _settings.proxyRotationEnabled();
-			return;
-		}
 		_proxyRotationValue = checked;
 		_controller->setProxyRotationEnabled(checked);
 		refreshProxyRotation();
@@ -1713,15 +1699,12 @@ void ProxiesBox::setupContent() {
 	if (!_proxyRotation || !_proxyRotationOptions) {
 		return;
 	}
-	const auto custom = _settings.isEnabled();
 	const auto visible = _settings.selected()
 		&& (_settings.list().size() > 1);
 	_proxyRotation->toggle(visible, anim::type::normal);
-	_proxyRotation->entity()->setDisabled(!custom);
 	_proxyRotationOptions->toggle(
 		visible && _proxyRotation->entity()->toggled(),
 		anim::type::normal);
-	_proxyRotationOptions->entity()->setDisabled(!custom);
 }
 
 void ProxiesBox::refreshProxySlowMode() {
@@ -1730,8 +1713,6 @@ void ProxiesBox::refreshProxySlowMode() {
 	}
 	_proxySlowModeOptionsLayout->toggle(true, anim::type::normal);
-	const auto active = _proxySlowMode->toggled()
-		&& _settings.isEnabled()
-		&& _controller->selectedUsesFakeTls();
+	const auto active = _proxySlowMode->toggled();
 	_proxySlowModeOptions->setDisabled(!active);
 }
 
@@ -1762,7 +1741,6 @@ void ProxiesBox::refreshCustomControls() {
 	if (!_proxySettings) {
 		return;
 	}
 	const auto custom = _settings.isEnabled();
-	const auto fakeTls = _controller->selectedUsesFakeTls();
 	const auto setButtonEnabled = [](Ui::SettingsButton *button, bool enabled) {
 		button->setDisabled(!enabled);
@@ -1779,31 +1760,30 @@ void ProxiesBox::refreshCustomControls() {
 	if (_proxyAddressPriorityButton) {
 		setButtonEnabled(
 			_proxyAddressPriorityButton,
-			custom
-				&& _settings.tryIPv6()
+			_settings.tryIPv6()
 				&& _controller->selectedUsesHostname());
 	}
 	if (_proxyCHelloButton) {
-		setButtonEnabled(_proxyCHelloButton, custom && fakeTls);
+		setButtonEnabled(_proxyCHelloButton, true);
 	}
 	if (_proxyCHelloStatus) {
-		_proxyCHelloStatus->setDisabled(!custom || !fakeTls);
+		_proxyCHelloStatus->setDisabled(false);
 	}
 	if (_proxySlowMode) {
-		setButtonEnabled(_proxySlowMode, custom && fakeTls);
+		setButtonEnabled(_proxySlowMode, true);
 	}
 	if (_proxySlowSummary) {
-		_proxySlowSummary->setDisabled(!custom || !fakeTls);
+		_proxySlowSummary->setDisabled(false);
 	}
 	if (_proxySlowDelayInput) {
 		setFieldEnabled(
 			_proxySlowDelayInput,
-			custom && fakeTls && _proxySlowMode->toggled());
+			_proxySlowMode->toggled());
 	}
 	if (_proxySlowJitterInput) {
 		setFieldEnabled(
 			_proxySlowJitterInput,
-			custom && fakeTls && _proxySlowMode->toggled());
+			_proxySlowMode->toggled());
 	}
 	if (_proxyForCalls) {
 		setButtonEnabled(_proxyForCalls->entity(), custom);
@@ -1814,7 +1794,7 @@ void ProxiesBox::refreshCustomControls() {
 	if (_proxyRotationTimeoutInput) {
 		setFieldEnabled(
 			_proxyRotationTimeoutInput,
-			custom && _proxyRotation->entity()->toggled());
+			_proxyRotation->entity()->toggled());
 	}
 	if (_refreshChecksButton) {
 		setButtonEnabled(
@@ -1822,7 +1802,7 @@ void ProxiesBox::refreshCustomControls() {
 			custom && _checksCaption.current() != u"Checking…"_q);
 	}
 	for (const auto &[id, row] : _rows) {
-		row->setSelectionEnabled(custom);
+		row->setSelectionEnabled(true);
 	}
 	refreshProxySlowMode();
 	refreshProxyRotation();
@@ -1934,9 +1914,7 @@ void ProxiesBox::createNoRowsLabel() {
 
 	button->clicks(
 	) | rpl::on_next([=] {
-		if (_settings.isEnabled()) {
-			_controller->applyItem(id);
-		}
+		_controller->applyItem(id);
 	}, button->lifetime());
 }
 
@@ -2805,17 +2783,19 @@ object_ptr<Ui::BoxContent> ProxiesBoxController::CreateOwningBox(
 
 void ProxiesBoxController::applyItem(int id) {
 	auto item = findById(id);
-	if (_settings.isEnabled() && (_settings.selected() == item->data)) {
-		return;
-	} else if (item->deleted) {
+	if (item->deleted || (_settings.selected() == item->data)) {
 		return;
 	}
 
 	auto j = findByProxy(_settings.selected());
-
-	Core::App().setCurrentProxy(
-		item->data,
-		ProxyData::Settings::Enabled);
+	if (_settings.isEnabled()) {
+		Core::App().setCurrentProxy(
+			item->data,
+			ProxyData::Settings::Enabled);
+	} else {
+		_settings.setSelected(item->data);
+		_settings.connectionTypeChangesNotify();
+	}
 	saveDelayed();
 
 	if (j != end(_list)) {
@@ -2997,9 +2977,6 @@ void ProxiesBoxController::replaceItemValue(
 }
 
 bool ProxiesBoxController::setProxyCHelloType(ProxyData::CHelloType value) {
-	if (!_settings.isEnabled()) {
-		return false;
-	}
 	if (_settings.proxyCHelloType() == value) {
 		return true;
 	}
@@ -3013,9 +2990,6 @@ bool ProxiesBoxController::setProxyCHelloType(ProxyData::CHelloType value) {
 }
 
 bool ProxiesBoxController::setProxySlowMode(bool value) {
-	if (!_settings.isEnabled()) {
-		return false;
-	}
 	if (_settings.proxySlowMode() == value) {
 		return true;
 	}
@@ -3026,10 +3000,9 @@ bool ProxiesBoxController::setProxySlowMode(bool value) {
 }
 
 bool ProxiesBoxController::setProxySlowDelay(int value) {
-	if (!_settings.isEnabled()) {
-		return false;
-	}
-	if (value < 0 || value > Core::SettingsProxy::kMaximumTimingValue) {
+	if (value < 0
+		|| value > Core::SettingsProxy::kMaximumTimingValue
+			- _settings.proxySlowJitter()) {
 		return false;
 	}
 	if (_settings.proxySlowDelay() == value) {
@@ -3042,10 +3015,9 @@ bool ProxiesBoxController::setProxySlowDelay(int value) {
 }
 
 bool ProxiesBoxController::setProxySlowJitter(int value) {
-	if (!_settings.isEnabled()) {
-		return false;
-	}
-	if (value < 0 || value > Core::SettingsProxy::kMaximumTimingValue) {
+	if (value < 0
+		|| value > Core::SettingsProxy::kMaximumTimingValue
+			- _settings.proxySlowDelay()) {
 		return false;
 	}
 	if (_settings.proxySlowJitter() == value) {
@@ -3058,9 +3030,6 @@ bool ProxiesBoxController::setProxySlowJitter(int value) {
 }
 
 void ProxiesBoxController::setProxyForCalls(bool enabled) {
-	if (!_settings.isEnabled()) {
-		return;
-	}
 	if (_settings.useProxyForCalls() == enabled) {
 		return;
 	}
@@ -3072,9 +3041,6 @@ void ProxiesBoxController::setProxyForCalls(bool enabled) {
 }
 
 void ProxiesBoxController::setProxyRotationEnabled(bool enabled) {
-	if (!_settings.isEnabled()) {
-		return;
-	}
 	if (_settings.proxyRotationEnabled() == enabled) {
 		return;
 	}
@@ -3083,9 +3049,6 @@ void ProxiesBoxController::setProxyRotationEnabled(bool enabled) {
 }
 
 void ProxiesBoxController::setProxyRotationTimeout(int value) {
-	if (!_settings.isEnabled()) {
-		return;
-	}
 	if (_settings.proxyRotationTimeout() == value) {
 		return;
 	}
@@ -3159,9 +3122,6 @@ void ProxiesBoxController::updateView(const Item &item) {
 }
 
 bool ProxiesBoxController::setProxyPreferIPv6(bool value) {
-	if (!_settings.isEnabled()) {
-		return false;
-	}
 	if (value && !_settings.tryIPv6()) {
 		return false;
 	}
@@ -3224,7 +3184,7 @@ auto ProxiesBoxController::viewForItem(const Item &item) const -> ItemView {
 		item.data.port,
 		item.ping,
 		item.checkedAt,
-		!deleted && selected && _settings.isEnabled(),
+		!deleted && selected,
 		deleted,
 		!deleted && supportsShare,
 		supportsCalls,
