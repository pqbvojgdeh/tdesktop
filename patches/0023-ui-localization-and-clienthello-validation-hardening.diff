diff --git a/Telegram/Resources/langs/lang.strings b/Telegram/Resources/langs/lang.strings
index dc610646e98f2ef04af9e4c95d9b4ec68e92ec23..4c34306a001ee14989bb6002d851da79470a2895 100644
--- a/Telegram/Resources/langs/lang.strings
+++ b/Telegram/Resources/langs/lang.strings
@@ -1299,6 +1299,51 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 "lng_proxy_add_from_clipboard_good_toast" = "Proxy was added from clipboard.";
 "lng_proxy_add_from_clipboard_failed_toast" = "This is not a proxy link.";
 "lng_proxy_add_from_clipboard_existing_toast" = "This proxy is already in the list.";
+"lng_proxy_disable_ipv6" = "Disable IPv6 for proxies";
+"lng_proxy_section_connection" = "Connection";
+"lng_proxy_prefer_ipv6" = "Prefer IPv6";
+"lng_proxy_prefer_ipv6_about" = "Hostnames only.";
+"lng_proxy_section_faketls" = "Global MTProto FakeTLS settings";
+"lng_proxy_faketls_about" = "Applied to every MTProto FakeTLS proxy.";
+"lng_proxy_client_hello_profile" = "ClientHello profile";
+"lng_proxy_chrome_profile" = "Google Chrome (Telegram profile)";
+"lng_proxy_firefox_permuted_profile" = "Mozilla Firefox 151 — permuted extensions";
+"lng_proxy_safari_profile" = "Apple Safari 26.4 (macOS)";
+"lng_proxy_yandex_gost_profile" = "Yandex Browser (GOST)";
+"lng_proxy_chromium_profile" = "Chromium 150";
+"lng_proxy_random_profile" = "Random on each connection";
+"lng_proxy_firefox_profile" = "Mozilla Firefox 151";
+"lng_proxy_ch_status_unused" = "Last connection\nNot used yet";
+"lng_proxy_ch_status_random" = "Random selection · ";
+"lng_proxy_ch_status_resumed" = " · resumed profile";
+"lng_proxy_ch_status_fresh" = " · fresh profile";
+"lng_proxy_ch_status_refresh" = "\nProfile refresh in {minutes} min";
+"lng_proxy_ch_status_resume_window" = "\nResume window: {minutes} min";
+"lng_proxy_ch_available_faketls" = "Available for MTProto FakeTLS proxies";
+"lng_proxy_ch_reconnecting" = "Reconnecting\nSelected profile: {profile}";
+"lng_proxy_section_connection_behavior" = "Connection behavior";
+"lng_proxy_slow_connect" = "Delay repeated connections";
+"lng_proxy_slow_range" = "Current range: {minimum}–{maximum} ms";
+"lng_proxy_slow_disabled" = "No additional delay";
+"lng_proxy_slow_minimum_delay" = "Minimum delay";
+"lng_proxy_slow_random_jitter" = "Random jitter";
+"lng_proxy_unit_ms" = "ms";
+"lng_proxy_section_servers" = "Proxy servers";
+"lng_proxy_rotation_timeout" = "Connection timeout";
+"lng_proxy_unit_seconds" = "s";
+"lng_proxy_rotation_about_custom" = "Switches after the selected proxy remains disconnected for this long.";
+"lng_proxy_refresh_checks" = "Refresh checks";
+"lng_proxy_check_completed" = "Completed";
+"lng_proxy_state_available" = "available";
+"lng_proxy_state_connected" = "connected";
+"lng_proxy_state_unavailable" = "unavailable";
+"lng_proxy_state_not_checked" = "not checked";
+"lng_proxy_rtt_measuring" = "Telegram RTT: measuring…";
+"lng_proxy_last_check_unavailable" = "Last check: unavailable";
+"lng_proxy_last_check_unavailable_at" = "Last check: unavailable · checked {time}";
+"lng_proxy_rtt_value" = "Telegram RTT: {ping} ms";
+"lng_proxy_rtt_value_checked" = "Telegram RTT: {ping} ms · checked {time}";
+"lng_build_metadata" = "Commit: {commit}\nBuild: {build}";
 "lng_badge_psa_default" = "PSA";
 "lng_about_psa_default" = "This message provides you with a public service announcement. To remove it from your chat list, right click it and select **Hide**.";
 "lng_tooltip_psa_default" = "This message provides you with a public service announcement.";
diff --git a/Telegram/SourceFiles/boxes/connection_box.cpp b/Telegram/SourceFiles/boxes/connection_box.cpp
index 3153d9e1448e4a6c7b4a23b802dcedc34eecf797..ee6206fcd8e6115650465606c1a9793502a145a7 100644
--- a/Telegram/SourceFiles/boxes/connection_box.cpp
+++ b/Telegram/SourceFiles/boxes/connection_box.cpp
@@ -11,6 +11,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "base/qt/qt_key_modifiers.h"
 #include "base/qthelp_regex.h"
 #include "base/qthelp_url.h"
+#include "base/timer.h"
 #include "base/weak_ptr.h"
 #include "core/application.h"
 #include "core/core_settings.h"
@@ -60,7 +61,6 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "styles/style_settings.h"
 
 #include <QtCore/QCollator>
-#include <QtCore/QTimer>
 #include <QtCore/QTime>
 #include <QtGui/QGuiApplication>
 #include <QtGui/QClipboard>
@@ -94,37 +94,37 @@ using ProxyData = MTP::ProxyData;
 [[nodiscard]] QString ClientHelloName(ProxyData::CHelloType type) {
 	switch (type) {
 	case ProxyData::CHelloType::Chrome:
-		return u"Google Chrome (Telegram profile)"_q;
+		return tr::lng_proxy_chrome_profile(tr::now);
 	case ProxyData::CHelloType::Custom:
-		return u"Mozilla Firefox 151 — permuted extensions"_q;
+		return tr::lng_proxy_firefox_permuted_profile(tr::now);
 	case ProxyData::CHelloType::SafariMac:
-		return u"Apple Safari 26.4 (macOS)"_q;
+		return tr::lng_proxy_safari_profile(tr::now);
 	case ProxyData::CHelloType::YandexGost:
-		return u"Yandex Browser (GOST)"_q;
+		return tr::lng_proxy_yandex_gost_profile(tr::now);
 	case ProxyData::CHelloType::Chromium150:
-		return u"Chromium 150"_q;
+		return tr::lng_proxy_chromium_profile(tr::now);
 	case ProxyData::CHelloType::RANDOM:
-		return u"Random on each connection"_q;
+		return tr::lng_proxy_random_profile(tr::now);
 	case ProxyData::CHelloType::Firefox:
 	default:
-		return u"Mozilla Firefox 151"_q;
+		return tr::lng_proxy_firefox_profile(tr::now);
 	}
 }
 
 [[nodiscard]] QString ClientHelloStatusText(const ProxyData &proxy) {
 	const auto status = ProxyData::globalCHelloStatus(proxy);
 	if (!status.used) {
-		return u"Last connection\nNot used yet"_q;
+		return tr::lng_proxy_ch_status_unused(tr::now);
 	}
 	auto result = QString();
 	if (status.random) {
-		result += u"Random selection · "_q;
+		result += tr::lng_proxy_ch_status_random(tr::now);
 	}
 	result += ClientHelloName(status.actual);
 	if (status.chromium150) {
 		result += status.resumed
-			? u" · resumed profile"_q
-			: u" · fresh profile"_q;
+			? tr::lng_proxy_ch_status_resumed(tr::now)
+			: tr::lng_proxy_ch_status_fresh(tr::now);
 	}
 	if (!status.host.isEmpty()) {
 		const auto actual = FormatProxyEndpoint(status.host, status.port);
@@ -137,18 +137,28 @@ using ProxyData = MTP::ProxyData;
 	}
 	if (status.chromium150 && status.ttlMinutes > 0) {
 		result += status.resumed
-			? u"\nProfile refresh in %1 min"_q.arg(status.ttlLeftMinutes)
-			: u"\nResume window: %1 min"_q.arg(status.ttlLeftMinutes);
+			? tr::lng_proxy_ch_status_refresh(
+				tr::now,
+				lt_minutes,
+				QString::number(status.ttlLeftMinutes))
+			: tr::lng_proxy_ch_status_resume_window(
+				tr::now,
+				lt_minutes,
+				QString::number(status.ttlLeftMinutes));
 	}
 	return result;
 }
 
 [[nodiscard]] QString SlowModeSummary(const Core::SettingsProxy &settings) {
 	return settings.proxySlowMode()
-		? u"Current range: %1–%2 ms"_q
-			.arg(settings.proxySlowDelay())
-			.arg(settings.proxySlowDelay() + settings.proxySlowJitter())
-		: u"No additional delay"_q;
+		? tr::lng_proxy_slow_range(
+			tr::now,
+			lt_minimum,
+			QString::number(settings.proxySlowDelay()),
+			lt_maximum,
+			QString::number(
+				settings.proxySlowDelay() + settings.proxySlowJitter()))
+		: tr::lng_proxy_slow_disabled(tr::now);
 }
 
 [[nodiscard]] std::vector<QString> ExtractLinkCandidates(const QString &input) {
@@ -710,7 +720,7 @@ private:
 	QPointer<Settings::Button> _proxyCHelloButton;
 	rpl::variable<QString> _proxyCHelloLabel;
 	QPointer<Ui::FlatLabel> _proxyCHelloStatus;
-	QPointer<QTimer> _proxyCHelloStatusTimer;
+	base::Timer _proxyCHelloStatusTimer;
 	QPointer<Ui::SlideWrap<Ui::SettingsButton>> _proxyForCalls;
 	QPointer<Ui::SlideWrap<Ui::SettingsButton>> _proxyRotation;
 	rpl::variable<bool> _proxyRotationValue;
@@ -725,8 +735,10 @@ private:
 	QPointer<Ui::MaskedInputField> _proxySlowDelayInput;
 	QPointer<Ui::MaskedInputField> _proxySlowJitterInput;
 	QPointer<Settings::Button> _refreshChecksButton;
-	rpl::variable<QString> _checksCaption = u"Not checked"_q;
+	rpl::variable<QString> _checksCaption
+		= tr::lng_proxy_state_not_checked(tr::now);
 	bool _checksStarted = false;
+	bool _checksRunning = false;
 	bool _proxyCHelloReconnectPending = false;
 	base::unique_qptr<Ui::RpWidget> _noRows;
 	object_ptr<Ui::VerticalLayout> _initialWrap;
@@ -965,12 +977,18 @@ void ProxyRow::paintEvent(QPaintEvent *e) {
 	p.setFont(st::normalFont);
 	const auto status = [&] {
 		switch (_view.state) {
-		case State::Available: return u"available"_q;
-		case State::Checking: return u"checking…"_q;
-		case State::Connecting: return u"connecting…"_q;
-		case State::Online: return u"connected"_q;
-		case State::Unavailable: return u"unavailable"_q;
-		case State::Inactive: return u"not checked"_q;
+		case State::Available:
+			return tr::lng_proxy_state_available(tr::now);
+		case State::Checking:
+			return tr::lng_proxy_checking(tr::now);
+		case State::Connecting:
+			return tr::lng_proxy_connecting(tr::now);
+		case State::Online:
+			return tr::lng_proxy_state_connected(tr::now);
+		case State::Unavailable:
+			return tr::lng_proxy_state_unavailable(tr::now);
+		case State::Inactive:
+			return tr::lng_proxy_state_not_checked(tr::now);
 		}
 		Unexpected("State in ProxyRow::paintEvent.");
 	}();
@@ -1007,16 +1025,26 @@ void ProxyRow::paintEvent(QPaintEvent *e) {
 	}
 	const auto detail = [&] {
 		if (_view.state == State::Checking) {
-			return u"Telegram RTT: measuring…"_q;
+			return tr::lng_proxy_rtt_measuring(tr::now);
 		} else if (_view.state == State::Unavailable) {
 			return _view.checkedAt.isEmpty()
-				? u"Last check: unavailable"_q
-				: u"Last check: unavailable · checked "_q + _view.checkedAt;
+				? tr::lng_proxy_last_check_unavailable(tr::now)
+				: tr::lng_proxy_last_check_unavailable_at(
+					tr::now,
+					lt_time,
+					_view.checkedAt);
 		} else if (_view.ping > 0) {
-			return u"Telegram RTT: %1 ms"_q.arg(_view.ping)
-				+ (_view.checkedAt.isEmpty()
-					? QString()
-					: u" · checked "_q + _view.checkedAt);
+			return _view.checkedAt.isEmpty()
+				? tr::lng_proxy_rtt_value(
+					tr::now,
+					lt_ping,
+					QString::number(_view.ping))
+				: tr::lng_proxy_rtt_value_checked(
+					tr::now,
+					lt_ping,
+					QString::number(_view.ping),
+					lt_time,
+					_view.checkedAt);
 		}
 		return QString();
 	}();
@@ -1103,23 +1131,23 @@ void ProxyRow::showMenu() {
 			const style::icon *icon) {
 		return _menu->addAction(text, std::move(callback), icon);
 	};
-	addAction(u"Edit"_q, [=] {
+	addAction(tr::lng_proxy_menu_edit(tr::now), [=] {
 		_editClicks.fire({});
 	}, &st::menuIconEdit);
 	if (_view.supportsShare) {
-		addAction(u"Share"_q, [=] {
+		addAction(tr::lng_proxy_edit_share(tr::now), [=] {
 			_shareClicks.fire({});
 		}, &st::menuIconShare);
-		addAction(u"QR code"_q, [=] {
+		addAction(tr::lng_group_invite_context_qr(tr::now), [=] {
 			_showQrClicks.fire({});
 		}, &st::menuIconQrCode);
 	}
 	if (_view.deleted) {
-		addAction(u"Restore"_q, [=] {
+		addAction(tr::lng_proxy_menu_restore(tr::now), [=] {
 			_restoreClicks.fire({});
 		}, &st::menuIconRestore);
 	} else {
-		addAction(u"Delete"_q, [=] {
+		addAction(tr::lng_proxy_menu_delete(tr::now), [=] {
 			_deleteClicks.fire({});
 		}, &st::menuIconDelete);
 	}
@@ -1163,6 +1191,7 @@ ProxiesBox::ProxiesBox(
 , _settings(settings)
 , _proxyAddressPriorityValue(settings.proxyPreferIPv6())
 , _proxyCHelloLabel(ClientHelloName(settings.proxyCHelloType()))
+, _proxyCHelloStatusTimer([=] { refreshProxyCHelloStatus(); })
 , _proxyRotationValue(settings.proxyRotationEnabled())
 , _proxySlowModeValue(settings.proxySlowMode())
 , _initialWrap(this)
@@ -1186,10 +1215,10 @@ void ProxiesBox::keyPressEvent(QKeyEvent *e) {
 }
 
 void ProxiesBox::prepare() {
-	setTitle(rpl::single(u"Proxy settings"_q));
+	setTitle(tr::lng_proxy_settings());
 
-	_addProxyButton = addButton(rpl::single(u"Add proxy"_q), [=] { addNewProxy(); });
-	addButton(rpl::single(u"Close"_q), [=] { closeBox(); });
+	_addProxyButton = addButton(tr::lng_proxy_add(), [=] { addNewProxy(); });
+	addButton(tr::lng_close(), [=] { closeBox(); });
 
 	setupTopButton();
 	setupContent();
@@ -1224,7 +1253,7 @@ void ProxiesBox::setupTopButton() {
 		const auto addAction = Ui::Menu::CreateAddActionCallback(raw);
 		const auto ipv6Disabled = !_settings.tryIPv6();
 		addAction({
-			.text = u"Disable IPv6 for proxies"_q,
+			.text = tr::lng_proxy_disable_ipv6(tr::now),
 			.handler = [=] {
 				_controller->setTryIPv6(ipv6Disabled);
 				_proxyAddressPriorityValue = _settings.proxyPreferIPv6();
@@ -1233,13 +1262,13 @@ void ProxiesBox::setupTopButton() {
 			.icon = ipv6Disabled ? &st::menuIconSelect : nullptr,
 		});
 		addAction({
-			.text = u"Add from clipboard"_q,
+			.text = tr::lng_proxy_add_from_clipboard(tr::now),
 			.handler = [=] { AddProxyFromClipboard(_controller, uiShow()); },
 			.icon = &st::menuIconImportTheme,
 		});
 		if (!_rows.empty()) {
 			addAction({
-				.text = u"Delete all"_q,
+				.text = tr::lng_group_invite_context_delete_all(tr::now),
 				.handler = [=] { _controller->deleteItems(); },
 				.icon = &st::menuIconDeleteAttention,
 				.isAttention = true,
@@ -1270,7 +1299,7 @@ void ProxiesBox::setupContent() {
 			st::proxyEditTitlePadding);
 	};
 
-	addSectionTitle(u"Connection"_q);
+	addSectionTitle(tr::lng_proxy_section_connection(tr::now));
 	_proxySettings
 		= std::make_shared<Ui::RadioenumGroup<ProxyData::Settings>>(
 			_settings.settings());
@@ -1279,46 +1308,46 @@ void ProxiesBox::setupContent() {
 			inner,
 			_proxySettings,
 			ProxyData::Settings::Disabled,
-			u"Disable proxy"_q),
+			tr::lng_proxy_disable(tr::now)),
 		st::proxyUsePadding);
 	inner->add(
 		object_ptr<Ui::Radioenum<ProxyData::Settings>>(
 			inner,
 			_proxySettings,
 			ProxyData::Settings::System,
-			u"Use system proxy settings"_q),
+			tr::lng_proxy_use_system_settings(tr::now)),
 		st::proxyUsePadding);
 	inner->add(
 		object_ptr<Ui::Radioenum<ProxyData::Settings>>(
 			inner,
 			_proxySettings,
 			ProxyData::Settings::Enabled,
-			u"Use custom proxy"_q),
+			tr::lng_proxy_use_custom(tr::now)),
 		st::proxyUsePadding);
 
 	_proxyAddressPriorityButton = Settings::AddButtonWithIcon(
 		inner,
-		rpl::single(u"Prefer IPv6"_q),
+		tr::lng_proxy_prefer_ipv6(),
 		st::settingsButtonNoIcon);
 	_proxyAddressPriorityButton->toggleOn(
 		_proxyAddressPriorityValue.value());
 	inner->add(
 		object_ptr<Ui::FlatLabel>(
 			inner,
-			u"Hostnames only."_q,
+			tr::lng_proxy_prefer_ipv6_about(tr::now),
 			st::boxDividerLabel),
 		st::proxyAboutPadding);
 
-	addSectionTitle(u"Global MTProto FakeTLS settings"_q);
+	addSectionTitle(tr::lng_proxy_section_faketls(tr::now));
 	inner->add(
 		object_ptr<Ui::FlatLabel>(
 			inner,
-			u"Applied to every MTProto FakeTLS proxy."_q,
+			tr::lng_proxy_faketls_about(tr::now),
 			st::boxDividerLabel),
 		st::proxyAboutPadding);
 	_proxyCHelloButton = Settings::AddButtonWithLabel(
 		inner,
-		rpl::single(u"ClientHello profile"_q),
+		tr::lng_proxy_client_hello_profile(),
 		_proxyCHelloLabel.value(),
 		st::settingsButtonNoIcon);
 	const auto clientMenu = _proxyCHelloButton->lifetime().make_state<
@@ -1369,17 +1398,11 @@ void ProxiesBox::setupContent() {
 			st::proxyUsePadding.right(),
 			st::proxyRowPadding.top()));
 	refreshProxyCHelloStatus();
-	_proxyCHelloStatusTimer = new QTimer(this);
-	_proxyCHelloStatusTimer->setInterval(1000);
-	connect(_proxyCHelloStatusTimer, &QTimer::timeout, [=] {
-		refreshProxyCHelloStatus();
-		_controller->refreshSelectedView();
-	});
-	_proxyCHelloStatusTimer->start();
-	addSectionTitle(u"Connection behavior"_q);
+	_proxyCHelloStatusTimer.callEach(crl::time(60 * 1000));
+	addSectionTitle(tr::lng_proxy_section_connection_behavior(tr::now));
 	_proxySlowMode = Settings::AddButtonWithIcon(
 		inner,
-		rpl::single(u"Delay repeated connections"_q),
+		tr::lng_proxy_slow_connect(),
 		st::settingsButtonNoIcon);
 	_proxySlowMode->toggleOn(_proxySlowModeValue.value());
 	_proxySlowSummary = inner->add(
@@ -1438,14 +1461,14 @@ void ProxiesBox::setupContent() {
 			return input;
 		};
 		_proxySlowDelayInput = addNumberRow(
-			u"Minimum delay"_q,
+			tr::lng_proxy_slow_minimum_delay(tr::now),
 			QString::number(_settings.proxySlowDelay()),
-			u"ms"_q,
+			tr::lng_proxy_unit_ms(tr::now),
 			Core::SettingsProxy::kMaximumTimingValue);
 		_proxySlowJitterInput = addNumberRow(
-			u"Random jitter"_q,
+			tr::lng_proxy_slow_random_jitter(tr::now),
 			QString::number(_settings.proxySlowJitter()),
-			u"ms"_q,
+			tr::lng_proxy_unit_ms(tr::now),
 			Core::SettingsProxy::kMaximumTimingValue);
 		_proxySlowModeOptions = _proxySlowModeOptionsLayout->entity();
 	}
@@ -1461,10 +1484,10 @@ void ProxiesBox::setupContent() {
 			std::move(callsButton)),
 		style::margins(0, 0, 0, st::proxyTryIPv6Padding.top()));
 
-	_about = addSectionTitle(u"Proxy servers"_q);
+	_about = addSectionTitle(tr::lng_proxy_section_servers(tr::now));
 	auto rotationButton = object_ptr<Ui::SettingsButton>(
 		inner,
-		rpl::single(u"Auto-switch proxies"_q),
+		tr::lng_proxy_auto_switch(),
 		st::settingsButtonNoIcon);
 	rotationButton->toggleOn(_proxyRotationValue.value());
 	_proxyRotation = inner->add(
@@ -1483,7 +1506,7 @@ void ProxiesBox::setupContent() {
 		st::proxyEditInputPadding);
 	const auto timeoutLabel = Ui::CreateChild<Ui::FlatLabel>(
 		timeoutRow,
-		u"Connection timeout"_q,
+		tr::lng_proxy_rotation_timeout(tr::now),
 		st::defaultFlatLabel);
 	_proxyRotationTimeoutInput = Ui::CreateChild<Ui::NumberInput>(
 		timeoutRow,
@@ -1493,7 +1516,7 @@ void ProxiesBox::setupContent() {
 		9999);
 	const auto timeoutSuffix = Ui::CreateChild<Ui::FlatLabel>(
 		timeoutRow,
-		u"s"_q,
+		tr::lng_proxy_unit_seconds(tr::now),
 		st::boxDividerLabel);
 	timeoutRow->widthValue() | rpl::on_next([=](int width) {
 		timeoutLabel->resizeToNaturalWidth(st::proxySettingsControlLabelWidth);
@@ -1517,13 +1540,13 @@ void ProxiesBox::setupContent() {
 	_proxyRotationOptions->entity()->add(
 		object_ptr<Ui::FlatLabel>(
 			_proxyRotationOptions->entity(),
-			u"Switches after the selected proxy remains disconnected for this long."_q,
+			tr::lng_proxy_rotation_about_custom(tr::now),
 			st::boxDividerLabel),
 		st::proxyAboutPadding);
 
 	_refreshChecksButton = Settings::AddButtonWithLabel(
 		inner,
-		rpl::single(u"Refresh checks"_q),
+		tr::lng_proxy_refresh_checks(),
 		_checksCaption.value(),
 		st::settingsButtonNoIcon);
 	_refreshChecksButton->addClickHandler([=] {
@@ -1626,13 +1649,14 @@ void ProxiesBox::setupContent() {
 		});
 	_controller->checksRunningValue(
 	) | rpl::on_next([=](bool running) {
+		_checksRunning = running;
 		if (running) {
 			_checksStarted = true;
-			_checksCaption = u"Checking…"_q;
+			_checksCaption = tr::lng_proxy_checking(tr::now);
 		} else {
 			_checksCaption = _checksStarted
-				? u"Completed"_q
-				: u"Not checked"_q;
+				? tr::lng_proxy_check_completed(tr::now)
+				: tr::lng_proxy_state_not_checked(tr::now);
 		}
 		refreshCustomControls();
 	}, inner->lifetime());
@@ -1659,7 +1683,7 @@ void ProxiesBox::setupContent() {
 				object_ptr<Ui::VerticalLayout>(inner)));
 		const auto shareList = Settings::AddButtonWithIcon(
 			wrap->entity(),
-			rpl::single(u"Share proxy list"_q),
+			tr::lng_proxy_edit_share_list_button(),
 			st::settingsButton,
 			{ &st::menuIconCopy });
 		_shareListButton = shareList;
@@ -1722,7 +1746,7 @@ void ProxiesBox::refreshProxyCHelloStatus() {
 	if (!_controller->selectedUsesFakeTls()) {
 		_proxyCHelloReconnectPending = false;
 		_proxyCHelloStatus->setText(
-			u"Available for MTProto FakeTLS proxies"_q);
+			tr::lng_proxy_ch_available_faketls(tr::now));
 		return;
 	}
 	const auto status = ProxyData::globalCHelloStatus(_settings.selected());
@@ -1730,8 +1754,10 @@ void ProxiesBox::refreshProxyCHelloStatus() {
 		&& (status.configured == _settings.proxyCHelloType());
 	if (_proxyCHelloReconnectPending && !currentProfileApplied) {
 		_proxyCHelloStatus->setText(
-			u"Reconnecting\nSelected profile: "_q
-			+ ClientHelloName(_settings.proxyCHelloType()));
+			tr::lng_proxy_ch_reconnecting(
+				tr::now,
+				lt_profile,
+				ClientHelloName(_settings.proxyCHelloType())));
 		return;
 	}
 	_proxyCHelloReconnectPending = false;
@@ -1797,7 +1823,7 @@ void ProxiesBox::refreshCustomControls() {
 	if (_refreshChecksButton) {
 		setButtonEnabled(
 			_refreshChecksButton,
-			custom && _checksCaption.current() != u"Checking…"_q);
+			custom && !_checksRunning);
 	}
 	for (const auto &[id, row] : _rows) {
 		row->setSelectionEnabled(true);
@@ -1878,7 +1904,7 @@ void ProxiesBox::createNoRowsLabel() {
 		_noRows->height());
 	const auto label = Ui::CreateChild<Ui::FlatLabel>(
 		_noRows.get(),
-		u"Add a proxy server to use a custom connection."_q,
+		tr::lng_proxy_description(tr::now),
 		st::proxyEmptyListLabel);
 	_noRows->widthValue(
 	) | rpl::on_next([=](int width) {
diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
index 1b8ddcab6424cf67d90087180b8ce47326a2a32e..09720315854ba10c5eaeff1dc5b3a310f6c3317b 100644
--- a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
@@ -1838,17 +1838,30 @@ bool ValidateClientHelloProfilesForTests() {
 	if (!check(PrepareClientHelloRules_Chromium150(true), 1, false, &sample)) {
 		return false;
 	}
-	// Deterministic mutation sweep: the validator may accept a mutation that
-	// touches random padding, but every mutation must be handled safely.
-	const auto mutationCount = std::min(int(sample.size()), 512);
-	for (auto i = 0; i != mutationCount; ++i) {
-		auto mutated = sample;
-		mutated[i] = char(uchar(mutated[i]) ^ uchar(0x5A));
-		(void)ValidateClientHello(
+	const auto validateSample = [&](const QByteArray &data, std::size_t size) {
+		return ValidateClientHello(
 			std::span<const std::byte>(
-				reinterpret_cast<const std::byte*>(mutated.constData()),
-				std::size_t(mutated.size())),
+				reinterpret_cast<const std::byte*>(data.constData()),
+				size),
 			1);
+	};
+	// Every truncated prefix must be rejected. This exercises every boundary
+	// without relying on a crash-only signal from unchecked mutations.
+	for (auto size = std::size_t(0);
+		size != std::size_t(sample.size());
+		++size) {
+		if (validateSample(sample, size).valid) {
+			return false;
+		}
+	}
+	// The TLS record and handshake envelope occupies bytes 0..8. Corrupting
+	// any byte in that envelope must produce a deterministic rejection.
+	for (auto i = 0; i != 9; ++i) {
+		auto mutated = sample;
+		mutated[i] = char(uchar(mutated[i]) ^ uchar(0x5A));
+		if (validateSample(mutated, std::size_t(mutated.size())).valid) {
+			return false;
+		}
 	}
 	return true;
 }
diff --git a/Telegram/SourceFiles/window/window_main_menu.cpp b/Telegram/SourceFiles/window/window_main_menu.cpp
index 23d7089cf14cbb92785472e6658e58fb6e2a40d7..fcb6d81bfdd251e30a07571b29ccb9bac9cf1a17 100644
--- a/Telegram/SourceFiles/window/window_main_menu.cpp
+++ b/Telegram/SourceFiles/window/window_main_menu.cpp
@@ -404,7 +404,12 @@ MainMenu::MainMenu(
 	const auto sourceCommitShort = u"__TDESKTOP_COMMIT_SHORT__"_q;
 	const auto sourceCommitUrl = u"https://github.com/telegramdesktop/tdesktop/commit/__TDESKTOP_COMMIT_FULL__"_q;
 	_version->setMarkedText(
-		tr::link(currentVersionText(), 1)
+		tr::link(
+			tr::lng_settings_current_version(
+				tr::now,
+				lt_version,
+				currentVersionText()),
+			1)
 			.append(u" · "_q)
 			.append(tr::link(sourceCommitShort, 2)));
 	_version->setLink(
diff --git a/Telegram/SourceFiles/window/window_main_menu_helpers.cpp b/Telegram/SourceFiles/window/window_main_menu_helpers.cpp
index 1f604869d4dcf615da2a992ee125b48a3eaa2d07..49010ef09fbaa689c8ec2d0dd129939aee3f6755 100644
--- a/Telegram/SourceFiles/window/window_main_menu_helpers.cpp
+++ b/Telegram/SourceFiles/window/window_main_menu_helpers.cpp
@@ -46,7 +46,12 @@ namespace Window {
 	if constexpr (!Platform::IsMacStoreBuild()
 		&& !Platform::IsWindowsStoreBuild()) {
 		Ui::InstallTooltip(label, [] {
-			return u"Commit: __TDESKTOP_COMMIT_TIME_UTC__\nBuild: __TDESKTOP_BUILD_TIME_UTC__"_q;
+			return tr::lng_build_metadata(
+				tr::now,
+				lt_commit,
+				u"__TDESKTOP_COMMIT_TIME_UTC__"_q,
+				lt_build,
+				u"__TDESKTOP_BUILD_TIME_UTC__"_q);
 		});
 	}
 	return label;
