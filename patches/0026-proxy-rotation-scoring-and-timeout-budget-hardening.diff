diff --git a/Telegram/SourceFiles/core/proxy_rotation_manager.cpp b/Telegram/SourceFiles/core/proxy_rotation_manager.cpp
index 93d78817853aa5992aff3a07c04b7419750c8ac4..496bae828ffa8774ec42ba01f806311053db251d 100644
--- a/Telegram/SourceFiles/core/proxy_rotation_manager.cpp
+++ b/Telegram/SourceFiles/core/proxy_rotation_manager.cpp
@@ -339,10 +339,9 @@ void ProxyRotationManager::checkDone(
 	entry->checking = false;
 	entry->checkedAt = crl::now();
 	entry->availableAt = crl::now();
-	entry->ping = std::max(ping, 1);
 	entry->health = details::ProxyRotationRecordSuccess(
 		entry->health,
-		entry->ping);
+		ping);
 	_failedRounds = 0;
 	const auto proxySettings = &App().settings().proxy();
 	if (const auto index = proxySettings->indexInList(proxy); index >= 0) {
@@ -366,7 +365,6 @@ void ProxyRotationManager::checkFailed(
 	entry->checking = false;
 	entry->checkedAt = crl::now();
 	entry->availableAt = 0;
-	entry->ping = 0;
 	entry->health = details::ProxyRotationRecordFailure(entry->health);
 	finishFailedRoundIfNeeded();
 	if (_waitingToSwitch && shouldSwitchToAvailable()) {
@@ -380,7 +378,7 @@ bool ProxyRotationManager::switchToAvailable() {
 	}
 	const auto &settings = App().settings().proxy();
 	auto bestIndex = -1;
-	auto bestScore = 0;
+	auto bestScore = std::int64_t(0);
 	const auto now = crl::now();
 	for (const auto index : _probeOrder) {
 		if (index < 0 || index >= int(settings.list().size())) {
@@ -388,18 +386,17 @@ bool ProxyRotationManager::switchToAvailable() {
 		}
 		const auto &proxy = settings.list()[index];
 		const auto entry = find(proxy);
-		if (!entry || entry->checking || !entry->availableAt) {
-			continue;
-		}
-		if (entry->availableAt < _switchStartedAt) {
+		if (!entry || !entry->availableAt) {
 			continue;
 		}
 		const auto ageSeconds = int(std::max(
 			(now - entry->availableAt) / crl::time(1000),
 			crl::time(0)));
+		if (!details::ProxyRotationResultIsFresh(ageSeconds)) {
+			continue;
+		}
 		const auto score = details::ProxyRotationSelectionScore(
 			entry->health,
-			entry->ping,
 			ageSeconds);
 		if (bestIndex < 0 || score < bestScore) {
 			bestIndex = index;
diff --git a/Telegram/SourceFiles/core/proxy_rotation_manager.h b/Telegram/SourceFiles/core/proxy_rotation_manager.h
index ac6afc648b261326c6b8b2b323a9094494256c41..c99f1fe5ab265c0ee24e3130b2b3cf7bfb7241ef 100644
--- a/Telegram/SourceFiles/core/proxy_rotation_manager.h
+++ b/Telegram/SourceFiles/core/proxy_rotation_manager.h
@@ -36,7 +36,6 @@ private:
 		bool checking = false;
 		crl::time checkedAt = 0;
 		crl::time availableAt = 0;
-		int ping = 0;
 		details::ProxyRotationHealth health;
 	};
 
diff --git a/Telegram/SourceFiles/core/proxy_rotation_policy.h b/Telegram/SourceFiles/core/proxy_rotation_policy.h
index d5b52a0df48b8d692715360e16d79daf35154eab..adca626e09a8311254285c721a8d46a96c08a5b3 100644
--- a/Telegram/SourceFiles/core/proxy_rotation_policy.h
+++ b/Telegram/SourceFiles/core/proxy_rotation_policy.h
@@ -8,6 +8,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #pragma once
 
 #include <algorithm>
+#include <cstdint>
 #include <cstdlib>
 
 namespace Core::details {
@@ -16,10 +17,13 @@ inline constexpr auto kProxyRotationSelectionPollMs = 100;
 inline constexpr auto kProxyRotationSelectionMaximumWaitMs = 2500;
 inline constexpr auto kProxyRotationFailurePenaltyStep = 250;
 inline constexpr auto kProxyRotationMaximumFailurePenalty = 2000;
+inline constexpr auto kProxyRotationFixedPointScale = 8;
+inline constexpr auto kProxyRotationDeviationWeight = 4;
+inline constexpr auto kProxyRotationMaximumResultAgeSeconds = 5 * 60;
 
 struct ProxyRotationHealth final {
-	int smoothedPing = 0;
-	int deviation = 0;
+	std::int64_t smoothedPingX8 = 0;
+	std::int64_t deviationX8 = 0;
 	int failurePenalty = 0;
 };
 
@@ -27,13 +31,21 @@ struct ProxyRotationHealth final {
 		ProxyRotationHealth health,
 		int ping) {
 	ping = std::max(ping, 1);
-	if (!health.smoothedPing) {
-		health.smoothedPing = ping;
-		health.deviation = 0;
+	const auto sampleX8 = std::int64_t(ping)
+		* kProxyRotationFixedPointScale;
+	if (!health.smoothedPingX8) {
+		health.smoothedPingX8 = sampleX8;
+		health.deviationX8 = 0;
 	} else {
-		const auto delta = std::abs(ping - health.smoothedPing);
-		health.deviation = (3 * health.deviation + delta) / 4;
-		health.smoothedPing = (3 * health.smoothedPing + ping) / 4;
+		const auto deltaX8 = std::abs(sampleX8 - health.smoothedPingX8);
+		health.deviationX8 = (
+			3 * health.deviationX8
+			+ deltaX8
+			+ 2) / 4;
+		health.smoothedPingX8 = (
+			3 * health.smoothedPingX8
+			+ sampleX8
+			+ 2) / 4;
 	}
 	health.failurePenalty /= 2;
 	return health;
@@ -44,20 +56,32 @@ struct ProxyRotationHealth final {
 	health.failurePenalty = std::min(
 		health.failurePenalty + kProxyRotationFailurePenaltyStep,
 		kProxyRotationMaximumFailurePenalty);
-	health.deviation = std::min(health.deviation + 50, 1000);
+	health.deviationX8 = std::min(
+		health.deviationX8
+			+ 50 * kProxyRotationFixedPointScale,
+		std::int64_t(1000 * kProxyRotationFixedPointScale));
 	return health;
 }
 
-[[nodiscard]] constexpr int ProxyRotationSelectionScore(
+[[nodiscard]] constexpr std::int64_t ProxyRotationSelectionScore(
 		const ProxyRotationHealth &health,
-		int ping,
 		int ageSeconds) {
-	const auto normalizedPing = std::max(ping, 1);
-	const auto agePenalty = std::clamp(ageSeconds * 5, 0, 100);
-	return normalizedPing
-		+ health.failurePenalty
-		+ (health.deviation / 2)
-		+ agePenalty;
+	const auto normalizedPingX8 = std::max(
+		health.smoothedPingX8,
+		std::int64_t(kProxyRotationFixedPointScale));
+	const auto agePenalty = std::clamp(
+		ageSeconds,
+		0,
+		kProxyRotationMaximumResultAgeSeconds);
+	return normalizedPingX8
+		+ std::int64_t(health.failurePenalty + agePenalty)
+			* kProxyRotationFixedPointScale
+		+ kProxyRotationDeviationWeight * health.deviationX8;
+}
+
+[[nodiscard]] constexpr bool ProxyRotationResultIsFresh(int ageSeconds) {
+	return ageSeconds >= 0
+		&& ageSeconds <= kProxyRotationMaximumResultAgeSeconds;
 }
 
 [[nodiscard]] constexpr bool ProxyRotationShouldKeepWaiting(
diff --git a/Telegram/SourceFiles/mtproto/connection_resolving.cpp b/Telegram/SourceFiles/mtproto/connection_resolving.cpp
index c2bb02a235f38b43f8a9916033101948ceea1e3b..e39353fd484405f7d6a30dc85bb31d7406f3eaae 100644
--- a/Telegram/SourceFiles/mtproto/connection_resolving.cpp
+++ b/Telegram/SourceFiles/mtproto/connection_resolving.cpp
@@ -22,12 +22,8 @@ namespace details {
 namespace {
 
 constexpr auto kOneConnectionTimeout = crl::time(4000);
-constexpr auto kSlowConnectMaximumWait = crl::time(
-	kSlowConnectMaximumAdditionalDelayMs);
-constexpr auto kSlowConnectSchedulingMargin = crl::time(500);
 constexpr auto kSlowOneConnectionTimeout = kOneConnectionTimeout
-	+ kSlowConnectMaximumWait
-	+ kSlowConnectSchedulingMargin;
+	+ crl::time(kSlowConnectMaximumTimeoutBudgetMs);
 constexpr auto kEarlyDisconnectQuarantine = 30 * crl::time(1000);
 constexpr auto kIpQuarantineBase = 30 * crl::time(1000);
 constexpr auto kIpQuarantineMaximum = 10 * crl::time(1000) * 60;
diff --git a/Telegram/SourceFiles/mtproto/connection_tcp.cpp b/Telegram/SourceFiles/mtproto/connection_tcp.cpp
index c2799cb872fed58ea275a0864855ec974033a370..db856631b7a2a7dc624851092f8bcfcbd119fbe4 100644
--- a/Telegram/SourceFiles/mtproto/connection_tcp.cpp
+++ b/Telegram/SourceFiles/mtproto/connection_tcp.cpp
@@ -22,7 +22,7 @@ namespace {
 constexpr auto kPacketSizeMax = int(0x01000000 * sizeof(mtpPrime));
 constexpr auto kFullConnectionTimeout = 8 * crl::time(1000);
 constexpr auto kSlowConnectTimeoutBudget = crl::time(
-	kSlowConnectMaximumAdditionalDelayMs);
+	kSlowConnectMaximumTimeoutBudgetMs);
 constexpr auto kSmallBufferSize = 256 * 1024;
 constexpr auto kMinPacketBuffer = 256;
 constexpr auto kConnectionStartPrefixSize = 64;
diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_slow_connect_policy.h b/Telegram/SourceFiles/mtproto/details/mtproto_slow_connect_policy.h
index 6a0127efc37b404c8575215b63a06650e953018f..02764b2db29f98aeb16632ef4e05e43ce68b1d3e 100644
--- a/Telegram/SourceFiles/mtproto/details/mtproto_slow_connect_policy.h
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_slow_connect_policy.h
@@ -13,6 +13,10 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 namespace MTP::details {
 
 inline constexpr auto kSlowConnectMaximumAdditionalDelayMs = 7500;
+inline constexpr auto kSlowConnectSchedulingMarginMs = 500;
+inline constexpr auto kSlowConnectMaximumTimeoutBudgetMs
+	= kSlowConnectMaximumAdditionalDelayMs
+	+ kSlowConnectSchedulingMarginMs;
 inline constexpr auto kSlowConnectMaximumPending = 16;
 inline constexpr auto kSlowConnectMaximumSlots = 128;
 inline constexpr auto kSlowConnectOverflowMinimumDelayMs = 100;
