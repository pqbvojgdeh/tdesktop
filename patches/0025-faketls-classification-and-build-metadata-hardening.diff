diff --git a/Telegram/Resources/langs/lang.strings b/Telegram/Resources/langs/lang.strings
index 083ba813fa3e4792a7a41f65d83fcfde872abfb0..cfddbe96ca920806d97f5bf6b52d0be52144decc 100644
--- a/Telegram/Resources/langs/lang.strings
+++ b/Telegram/Resources/langs/lang.strings
@@ -1343,7 +1343,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 "lng_proxy_last_check_unavailable_at" = "Last check: unavailable · checked {time}";
 "lng_proxy_rtt_value" = "Telegram RTT: {ping} ms";
 "lng_proxy_rtt_value_checked" = "Telegram RTT: {ping} ms · checked {time}";
-"lng_build_metadata" = "Commit: {commit}\nBuild: {build}";
+"lng_build_metadata" = "Commit: {commit}\nBuild: {build}\nQt: {qt}";
 "lng_badge_psa_default" = "PSA";
 "lng_about_psa_default" = "This message provides you with a public service announcement. To remove it from your chat list, right click it and select **Hide**.";
 "lng_tooltip_psa_default" = "This message provides you with a public service announcement.";
diff --git a/Telegram/SourceFiles/boxes/connection_box.cpp b/Telegram/SourceFiles/boxes/connection_box.cpp
index ee6206fcd8e6115650465606c1a9793502a145a7..8694f19ec9f362b5e1b82bdeb4cbe9b472d72495 100644
--- a/Telegram/SourceFiles/boxes/connection_box.cpp
+++ b/Telegram/SourceFiles/boxes/connection_box.cpp
@@ -77,14 +77,6 @@ constexpr auto kSaveSettingsDelayedTimeout = crl::time(1000);
 
 using ProxyData = MTP::ProxyData;
 
-[[nodiscard]] bool UsesFakeTls(const ProxyData &proxy) {
-	if (proxy.type != ProxyData::Type::Mtproto) {
-		return false;
-	}
-	const auto secret = proxy.secretFromMtprotoPassword();
-	return secret.size() >= 21 && secret[0] == bytes::type(0xEE);
-}
-
 [[nodiscard]] QString FormatProxyEndpoint(const QString &host, uint32 port) {
 	return qthelp::is_ipv6(host)
 		? u"[%1]:%2"_q.arg(host).arg(port)
@@ -2549,7 +2541,7 @@ std::vector<ProxiesBoxController::ItemView> ProxiesBoxController::snapshot() con
 }
 
 bool ProxiesBoxController::selectedUsesFakeTls() const {
-	return UsesFakeTls(_settings.selected());
+	return _settings.selected().usesFakeTls();
 }
 
 bool ProxiesBoxController::selectedUsesHostname() const {
diff --git a/Telegram/SourceFiles/mtproto/connection_resolving.cpp b/Telegram/SourceFiles/mtproto/connection_resolving.cpp
index 9f9c1e0ddfc680eb333c35a5d6b55b81091e7f24..c2bb02a235f38b43f8a9916033101948ceea1e3b 100644
--- a/Telegram/SourceFiles/mtproto/connection_resolving.cpp
+++ b/Telegram/SourceFiles/mtproto/connection_resolving.cpp
@@ -51,16 +51,8 @@ struct IpHealthEntry {
 	int failures = 0;
 };
 
-[[nodiscard]] bool UsesFakeTls(const ProxyData &proxy) {
-	if (proxy.type != ProxyData::Type::Mtproto) {
-		return false;
-	}
-	const auto secret = proxy.secretFromMtprotoPassword();
-	return secret.size() > 17 && secret.front() == bytes::type(0xEE);
-}
-
 [[nodiscard]] crl::time OneConnectionTimeout(const ProxyData &proxy) {
-	return ProxyData::globalSlowMode() && UsesFakeTls(proxy)
+	return ProxyData::globalSlowMode() && proxy.usesFakeTls()
 		? kSlowOneConnectionTimeout
 		: kOneConnectionTimeout;
 }
diff --git a/Telegram/SourceFiles/mtproto/connection_tcp.cpp b/Telegram/SourceFiles/mtproto/connection_tcp.cpp
index c0462eeae659d79165594328713149defef0cb94..c2799cb872fed58ea275a0864855ec974033a370 100644
--- a/Telegram/SourceFiles/mtproto/connection_tcp.cpp
+++ b/Telegram/SourceFiles/mtproto/connection_tcp.cpp
@@ -8,6 +8,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "mtproto/connection_tcp.h"
 
 #include "mtproto/details/mtproto_abstract_socket.h"
+#include "mtproto/details/mtproto_fake_tls_policy.h"
 #include "mtproto/details/mtproto_slow_connect_policy.h"
 #include "base/bytes.h"
 #include "base/openssl_help.h"
@@ -235,7 +236,7 @@ QString TcpConnection::Protocol::VersionD::debugPostfix() const {
 auto TcpConnection::Protocol::Create(bytes::const_span secret)
 -> std::unique_ptr<Protocol> {
 	// See also DcOptions::ValidateSecret.
-	if ((secret.size() >= 21 && secret[0] == bytes::type(0xEE))
+	if (IsFakeTlsSecret(secret)
 		|| (secret.size() == 17 && secret[0] == bytes::type(0xDD))) {
 		return std::make_unique<VersionD>(
 			bytes::make_vector(secret.subspan(1, 16)));
@@ -582,14 +583,8 @@ crl::time TcpConnection::pingTime() const {
 }
 
 crl::time TcpConnection::fullConnectTimeout() const {
-	if (ProxyData::globalSlowMode()
-		&& _proxy.type == ProxyData::Type::Mtproto) {
-		const auto secret = _proxy.secretFromMtprotoPassword();
-		if (secret.size() >= 21
-			&& secret.front() == bytes::type(0xEE)) {
-			return kFullConnectionTimeout
-				+ kSlowConnectTimeoutBudget;
-		}
+	if (ProxyData::globalSlowMode() && _proxy.usesFakeTls()) {
+		return kFullConnectionTimeout + kSlowConnectTimeoutBudget;
 	}
 	return kFullConnectionTimeout;
 }
diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_fake_tls_policy.h b/Telegram/SourceFiles/mtproto/details/mtproto_fake_tls_policy.h
new file mode 100644
index 0000000000000000000000000000000000000000..796a0d6e8aac2f93b20fca4b131c5db7e62afd58
--- /dev/null
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_fake_tls_policy.h
@@ -0,0 +1,26 @@
+/*
+This file is part of Telegram Desktop,
+the official desktop application for the Telegram messaging service.
+
+For license and copyright information please follow this link:
+https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
+*/
+#pragma once
+
+#include <cstddef>
+#include <type_traits>
+
+namespace MTP::details {
+
+// 0xEE + 16 secret bytes + at least four domain bytes, matching
+// ProxyData validation and TcpConnection::Protocol::Create.
+inline constexpr auto kFakeTlsMinimumSecretSize = std::size_t(21);
+
+template <typename Span>
+[[nodiscard]] constexpr bool IsFakeTlsSecret(const Span &secret) {
+	using Byte = std::remove_cvref_t<decltype(secret.front())>;
+	return secret.size() >= kFakeTlsMinimumSecretSize
+		&& secret.front() == Byte(0xEE);
+}
+
+} // namespace MTP::details
diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
index 3a7550cda7ba435742598289621baca6aa9150ed..d0c1f958bbb9f96fb09ed0b8dcf85e50f9dadc92 100644
--- a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
@@ -8,6 +8,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "mtproto/details/mtproto_tls_socket.h"
 
 #include "mtproto/details/mtproto_client_hello_validation.h"
+#include "mtproto/details/mtproto_fake_tls_policy.h"
 #include "mtproto/details/mtproto_slow_connect_policy.h"
 #include "mtproto/details/mtproto_chromium_resumption_cache.h"
 
@@ -1892,7 +1893,7 @@ TlsSocket::TlsSocket(
 	bool protocolForFiles)
 : AbstractSocket(thread)
 , _secret(secret) {
-	Expects(_secret.size() >= 21 && _secret[0] == bytes::type(0xEE));
+	Expects(IsFakeTlsSecret(_secret));
 
 	_socket.moveToThread(thread);
 	_socket.setProxy(proxy);
diff --git a/Telegram/SourceFiles/mtproto/mtproto_proxy_data.cpp b/Telegram/SourceFiles/mtproto/mtproto_proxy_data.cpp
index 443b7c6fa71f9bad023c3cc27ed460e4a94c74b4..f7b7d829a445e7e546bbfd4d3d56a2b120a98457 100644
--- a/Telegram/SourceFiles/mtproto/mtproto_proxy_data.cpp
+++ b/Telegram/SourceFiles/mtproto/mtproto_proxy_data.cpp
@@ -10,6 +10,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "base/openssl_help.h"
 #include "base/qthelp_url.h"
 #include "base/qt/qt_string_view.h"
+#include "mtproto/details/mtproto_fake_tls_policy.h"
 #include "mtproto/details/mtproto_slow_connect_policy.h"
 
 #include <QtCore/QMutex>
@@ -38,13 +39,10 @@ constexpr auto kMaximumSlowTimingValue = details::kSlowConnectMaximumAdditionalD
 }
 
 [[nodiscard]] QByteArray DomainId(const ProxyData &proxy) {
-	if (proxy.type != ProxyData::Type::Mtproto) {
+	if (!proxy.usesFakeTls()) {
 		return {};
 	}
 	const auto secret = proxy.secretFromMtprotoPassword();
-	if (secret.size() <= 17 || secret.front() != bytes::type(0xEE)) {
-		return {};
-	}
 	return DomainId(bytes::make_span(secret).subspan(17));
 }
 
@@ -220,6 +218,11 @@ bytes::vector ProxyData::secretFromMtprotoPassword() const {
 	return {};
 }
 
+bool ProxyData::usesFakeTls() const {
+	return type == Type::Mtproto
+		&& details::IsFakeTlsSecret(secretFromMtprotoPassword());
+}
+
 ProxyData::operator bool() const {
 	return valid();
 }
diff --git a/Telegram/SourceFiles/mtproto/mtproto_proxy_data.h b/Telegram/SourceFiles/mtproto/mtproto_proxy_data.h
index 2d80433c87b4b383b49dd679d36fcb06aba4e43a..ac0c0561f28ebcb7d02858f873c8e3ec380c5a50 100644
--- a/Telegram/SourceFiles/mtproto/mtproto_proxy_data.h
+++ b/Telegram/SourceFiles/mtproto/mtproto_proxy_data.h
@@ -68,6 +68,7 @@ struct ProxyData {
 	[[nodiscard]] bool supportsCalls() const;
 	[[nodiscard]] bool tryCustomResolve() const;
 	[[nodiscard]] bytes::vector secretFromMtprotoPassword() const;
+	[[nodiscard]] bool usesFakeTls() const;
 	[[nodiscard]] explicit operator bool() const;
 	[[nodiscard]] bool operator==(const ProxyData &other) const;
 	[[nodiscard]] bool operator!=(const ProxyData &other) const;
diff --git a/Telegram/SourceFiles/mtproto/session_private.cpp b/Telegram/SourceFiles/mtproto/session_private.cpp
index 3f085e90e66c933e2a4cf6fb99fb996741e7ad69..28e52284b877fafb1421a9b897b94dd62abc3b76 100644
--- a/Telegram/SourceFiles/mtproto/session_private.cpp
+++ b/Telegram/SourceFiles/mtproto/session_private.cpp
@@ -1111,15 +1111,8 @@ void SessionPrivate::connectToServer(bool afterConfig) {
 
 	auto waitForConnected = _waitForConnected;
 	const auto &proxy = _options->proxy;
-	const auto slowFakeTls = [&] {
-		if (!ProxyData::globalSlowMode()
-			|| proxy.type != ProxyData::Type::Mtproto) {
-			return false;
-		}
-		const auto secret = proxy.secretFromMtprotoPassword();
-		return secret.size() > 17
-			&& secret.front() == bytes::type(0xEE);
-	}();
+	const auto slowFakeTls = ProxyData::globalSlowMode()
+		&& proxy.usesFakeTls();
 	if (slowFakeTls) {
 		for (const auto &connection : _testConnections) {
 			accumulate_max(
diff --git a/Telegram/SourceFiles/window/window_main_menu.cpp b/Telegram/SourceFiles/window/window_main_menu.cpp
index fcb6d81bfdd251e30a07571b29ccb9bac9cf1a17..c6d71e0caa13f01387d86d916f21df41a6d89d8b 100644
--- a/Telegram/SourceFiles/window/window_main_menu.cpp
+++ b/Telegram/SourceFiles/window/window_main_menu.cpp
@@ -401,23 +401,36 @@ MainMenu::MainMenu(
 		}
 
 	};
-	const auto sourceCommitShort = u"__TDESKTOP_COMMIT_SHORT__"_q;
-	const auto sourceCommitUrl = u"https://github.com/telegramdesktop/tdesktop/commit/__TDESKTOP_COMMIT_FULL__"_q;
-	_version->setMarkedText(
-		tr::link(
-			tr::lng_settings_current_version(
-				tr::now,
-				lt_version,
-				currentVersionText()),
-			1)
+	const auto sourceCommitShort = ResolveEmbeddedBuildMetadata(
+		u"__TDESKTOP_COMMIT_SHORT__"_q,
+		QString());
+	const auto sourceCommitFull = ResolveEmbeddedBuildMetadata(
+		u"__TDESKTOP_COMMIT_FULL__"_q,
+		QString());
+	const auto hasSourceCommit = !sourceCommitShort.isEmpty()
+		&& !sourceCommitFull.isEmpty();
+	auto versionText = tr::link(
+		tr::lng_settings_current_version(
+			tr::now,
+			lt_version,
+			currentVersionText()),
+		1);
+	if (hasSourceCommit) {
+		versionText
 			.append(u" · "_q)
-			.append(tr::link(sourceCommitShort, 2)));
+			.append(tr::link(sourceCommitShort, 2));
+	}
+	_version->setMarkedText(versionText);
 	_version->setLink(
 		1,
 		std::make_shared<UrlClickHandler>(Core::App().changelogLink()));
-	_version->setLink(
-		2,
-		std::make_shared<SourceCommitClickHandler>(sourceCommitUrl));
+	if (hasSourceCommit) {
+		const auto sourceCommitUrl = u"https://github.com/telegramdesktop/tdesktop/commit/"_q
+			+ sourceCommitFull;
+		_version->setLink(
+			2,
+			std::make_shared<SourceCommitClickHandler>(sourceCommitUrl));
+	}
 	_about->setMarkedText(tr::link(tr::lng_menu_about(tr::now), 1));
 	_about->setLink(
 		1,
diff --git a/Telegram/SourceFiles/window/window_main_menu_helpers.cpp b/Telegram/SourceFiles/window/window_main_menu_helpers.cpp
index 49010ef09fbaa689c8ec2d0dd129939aee3f6755..14bc88acff4e004b37e70241fd29e5924366bbc6 100644
--- a/Telegram/SourceFiles/window/window_main_menu_helpers.cpp
+++ b/Telegram/SourceFiles/window/window_main_menu_helpers.cpp
@@ -38,6 +38,14 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 
 namespace Window {
 
+QString ResolveEmbeddedBuildMetadata(
+		QString embedded,
+		QString fallback) {
+	const auto placeholder = embedded.startsWith(u"__TDESKTOP_"_q)
+		&& embedded.endsWith(u"__"_q);
+	return placeholder ? fallback : embedded;
+}
+
 [[nodiscard]] not_null<Ui::FlatLabel*> AddVersionLabel(
 		not_null<Ui::RpWidget*> parent) {
 	const auto label = Ui::CreateChild<Ui::FlatLabel>(
@@ -49,9 +57,15 @@ namespace Window {
 			return tr::lng_build_metadata(
 				tr::now,
 				lt_commit,
-				u"__TDESKTOP_COMMIT_TIME_UTC__"_q,
+				ResolveEmbeddedBuildMetadata(
+					u"__TDESKTOP_COMMIT_TIME_UTC__"_q,
+					u"unknown"_q),
 				lt_build,
-				u"__TDESKTOP_BUILD_TIME_UTC__"_q);
+				ResolveEmbeddedBuildMetadata(
+					u"__TDESKTOP_BUILD_TIME_UTC__"_q,
+					QString::fromLatin1(__DATE__ " " __TIME__)),
+				lt_qt,
+				QString::fromLatin1(QT_VERSION_STR));
 		});
 	}
 	return label;
diff --git a/Telegram/SourceFiles/window/window_main_menu_helpers.h b/Telegram/SourceFiles/window/window_main_menu_helpers.h
index 94da85e96087c1f6a87d52c84ec336b0bb38ff36..fe7ab69002c86b86bdcd1ac2eb654c18c8d8c8b0 100644
--- a/Telegram/SourceFiles/window/window_main_menu_helpers.h
+++ b/Telegram/SourceFiles/window/window_main_menu_helpers.h
@@ -7,6 +7,8 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 */
 #pragma once
 
+class QString;
+
 namespace Ui {
 class FlatLabel;
 class RpWidget;
@@ -18,5 +20,9 @@ namespace Window {
 
 class SessionController;
 
+[[nodiscard]] QString ResolveEmbeddedBuildMetadata(
+	QString embedded,
+	QString fallback);
+
 [[nodiscard]] not_null<Ui::FlatLabel*> AddVersionLabel(
 	not_null<Ui::RpWidget*>);
