diff --git a/Telegram/CMakeLists.txt b/Telegram/CMakeLists.txt
--- a/Telegram/CMakeLists.txt
+++ b/Telegram/CMakeLists.txt
@@ -89,16 +89,19 @@ PRIVATE
     desktop-app::external_openal
     desktop-app::external_xxhash
 )
 
 if (DESKTOP_APP_MTPROTO_PROFILE_TESTS)
     target_compile_definitions(Telegram PRIVATE
         DESKTOP_APP_MTPROTO_PROFILE_TESTS
     )
+    target_compile_definitions(td_mtproto PRIVATE
+        DESKTOP_APP_MTPROTO_PROFILE_TESTS
+    )
 endif()
 
 if (NOT DESKTOP_APP_DISABLE_SWIFT6)
     telegram_add_apple_swift_runtime(Telegram)
 endif()
 
 target_precompile_headers(Telegram PRIVATE $<$<COMPILE_LANGUAGE:CXX,OBJCXX>:${src_loc}/stdafx.h>)
 nice_target_sources(Telegram ${src_loc}
diff --git a/Telegram/SourceFiles/main.cpp b/Telegram/SourceFiles/main.cpp
--- a/Telegram/SourceFiles/main.cpp
+++ b/Telegram/SourceFiles/main.cpp
@@ -3,23 +3,21 @@ This file is part of Telegram Desktop,
 the official desktop application for the Telegram messaging service.
 
 For license and copyright information please follow this link:
 https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 */
 #include "core/launcher.h"
 
 #ifdef DESKTOP_APP_MTPROTO_PROFILE_TESTS
+#include "mtproto/details/mtproto_tls_socket.h"
+
 #include <QtCore/QCoreApplication>
 
 #include <cstring>
-
-namespace MTP::details {
-[[nodiscard]] bool ValidateClientHelloProfilesForTests();
-} // namespace MTP::details
 #endif
 
 int main(int argc, char *argv[]) {
 #ifdef DESKTOP_APP_MTPROTO_PROFILE_TESTS
 	if (argc == 2
 		&& !std::strcmp(argv[1], "--validate-mtproto-client-hello")) {
 		const auto application = QCoreApplication(argc, argv);
 		return MTP::details::ValidateClientHelloProfilesForTests() ? 0 : 1;
diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.h b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.h
--- a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.h
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.h
@@ -6,17 +6,19 @@ For license and copyright information please follow this link:
 https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 */
 #pragma once
 
 #include "mtproto/details/mtproto_abstract_socket.h"
 
 namespace MTP::details {
 
+#ifdef DESKTOP_APP_MTPROTO_PROFILE_TESTS
 [[nodiscard]] bool ValidateClientHelloProfilesForTests();
+#endif
 
 class TlsSocket final : public AbstractSocket {
 public:
 	TlsSocket(
 		not_null<QThread*> thread,
 		const bytes::vector &secret,
 		const QNetworkProxy &proxy,
 		bool protocolForFiles);
diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
--- a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
@@ -177,16 +177,17 @@ struct SlowConnectSchedule {
 		}
 	} else {
 		// Do not turn local scheduler pressure into a simultaneous TCP burst.
 		result.waitFor = overflowDelay();
 	}
 	return result;
 }
 
+#ifdef DESKTOP_APP_MTPROTO_PROFILE_TESTS
 [[nodiscard]] bool ValidateSlowConnectReservationForTests() {
 	const auto endpoint = SlowConnectEndpoint(
 		u"slow-connect-reservation-test.invalid"_q,
 		1);
 	{
 		const QMutexLocker lock(&SlowConnectMutex());
 		SlowConnectSlots()[endpoint] = {
 			sc::steady_clock::now() - sc::milliseconds(1),
@@ -195,16 +196,17 @@ struct SlowConnectSchedule {
 	}
 	{
 		const auto reservation = std::make_shared<SlowConnectReservation>(
 			endpoint);
 	}
 	const QMutexLocker lock(&SlowConnectMutex());
 	return !SlowConnectSlots().contains(endpoint);
 }
+#endif // DESKTOP_APP_MTPROTO_PROFILE_TESTS
 
 using BigNum = openssl::BigNum;
 using BigNumContext = openssl::Context;
 
 [[nodiscard]] MTPTlsClientHello PrepareClientHelloRules_Chrome() {
 	using Scope = QVector<MTPTlsBlock>;
 	using Permutation = std::vector<Scope>;
 	using StackElement = std::variant<Scope, Permutation>;
@@ -1787,16 +1789,17 @@ ClientHello Generator::take() {
 [[nodiscard]] int ReadPartLength(bytes::const_span data, int offset) {
 	const auto storage = data.subspan(offset, kLengthSize);
 	return qFromBigEndian(
 		*reinterpret_cast<const uint16*>(storage.data()));
 }
 
 } // namespace
 
+#ifdef DESKTOP_APP_MTPROTO_PROFILE_TESTS
 bool ValidateClientHelloProfilesForTests() {
 	if (!ValidateSlowConnectReservationForTests()) {
 		return false;
 	}
 	const auto domain = QByteArray("example.com");
 	const auto key = bytes::vector(16, bytes::type(0x42));
 	const auto check = [&](const MTPTlsClientHello &rules,
 			int expectedResumed,
@@ -1844,16 +1847,17 @@ bool ValidateClientHelloProfilesForTests() {
 		(void)ValidateClientHello(
 			std::span<const std::byte>(
 				reinterpret_cast<const std::byte*>(mutated.constData()),
 				std::size_t(mutated.size())),
 			1);
 	}
 	return true;
 }
+#endif // DESKTOP_APP_MTPROTO_PROFILE_TESTS
 
 TlsSocket::TlsSocket(
 	not_null<QThread*> thread,
 	const bytes::vector &secret,
 	const QNetworkProxy &proxy,
 	bool protocolForFiles)
 : AbstractSocket(thread)
 , _secret(secret) {
diff --git a/Telegram/SourceFiles/core/core_settings_proxy.cpp b/Telegram/SourceFiles/core/core_settings_proxy.cpp
--- a/Telegram/SourceFiles/core/core_settings_proxy.cpp
+++ b/Telegram/SourceFiles/core/core_settings_proxy.cpp
@@ -21,30 +21,30 @@ namespace Core {
 	case MTP::ProxyData::CHelloType::RANDOM:  return -1;
 	case MTP::ProxyData::CHelloType::Firefox: return 0;
 	case MTP::ProxyData::CHelloType::Chrome:  return 1;
 	case MTP::ProxyData::CHelloType::Custom:  return 2;
 	case MTP::ProxyData::CHelloType::SafariMac: return 3;
 	case MTP::ProxyData::CHelloType::YandexGost: return 4;
 	case MTP::ProxyData::CHelloType::Chromium150: return 5;
 	}
-	return 5;
+	return -1;
 }
 
 [[nodiscard]] MTP::ProxyData::CHelloType IntToProxyCHelloType(qint32 value) {
 	switch(value) {
 	case -1: return MTP::ProxyData::CHelloType::RANDOM;
 	case 0: return MTP::ProxyData::CHelloType::Firefox;
 	case 1: return MTP::ProxyData::CHelloType::Chrome;
 	case 2: return MTP::ProxyData::CHelloType::Custom;
 	case 3: return MTP::ProxyData::CHelloType::SafariMac;
 	case 4: return MTP::ProxyData::CHelloType::YandexGost;
 	case 5: return MTP::ProxyData::CHelloType::Chromium150;
 	}
-	return MTP::ProxyData::CHelloType::Chromium150;
+	return MTP::ProxyData::CHelloType::RANDOM;
 }
 
 namespace {
 
 [[nodiscard]] qint32 ProxySettingsToInt(MTP::ProxyData::Settings settings) {
 	switch(settings) {
 	case MTP::ProxyData::Settings::System: return 0;
 	case MTP::ProxyData::Settings::Enabled: return 1;
@@ -282,89 +282,109 @@ bool SettingsProxy::setFromSerialized(const QByteArray &serialized) {
 			preferredIndices.reserve(preferredCount);
 			for (auto i = 0; i != preferredCount; ++i) {
 				auto index = qint32(0);
 				stream >> index;
 				preferredIndices.push_back(index);
 			}
 		}
 	}
+	if (!stream.ok()) {
+		LOG(("App Error: "
+			"Bad data for Core::SettingsProxy::setFromSerialized()"));
+		return false;
+	}
 	qint32 proxyCHtype = ProxyCHelloTypeToInt(_ch_type);
 	qint32 proxySlowMode = qint32(_proxySlowMode ? 1 : 0);
 	qint32 proxySlowDelay = _proxySlowDelay;
 	qint32 proxySlowJitter = _proxySlowJitter;
 	auto proxyPreferIPv6 = qint32(_proxyPreferIPv6 ? 1 : 0);
+	const auto resetExtensionFields = [&] {
+		proxyCHtype = ProxyCHelloTypeToInt(_ch_type);
+		proxySlowMode = qint32(_proxySlowMode ? 1 : 0);
+		proxySlowDelay = _proxySlowDelay;
+		proxySlowJitter = _proxySlowJitter;
+		proxyPreferIPv6 = qint32(_proxyPreferIPv6 ? 1 : 0);
+	};
+	auto invalidExtension = false;
 	if (!stream.atEnd()) {
 		auto marker = qint32(0);
 		stream >> marker;
 		if (!stream.ok()) {
-			return false;
-		}
-		if (marker == kProxySettingsExtensionMagic) {
+			invalidExtension = true;
+		} else if (marker == kProxySettingsExtensionMagic) {
 			auto version = qint32(0);
 			stream >> version;
 			if (!stream.ok()) {
-				return false;
-			}
-			if (version == details::kProxySettingsLegacyExtensionVersion) {
+				invalidExtension = true;
+			} else if (version == details::kProxySettingsLegacyExtensionVersion) {
 				// Version 1 had no payload length and must stay readable.
 				stream
 					>> proxyCHtype
 					>> proxySlowMode
 					>> proxySlowDelay
 					>> proxySlowJitter
 					>> proxyPreferIPv6;
+				invalidExtension = !stream.ok();
 			} else if (version >= kProxySettingsExtensionVersion) {
 				auto extension = QByteArray();
 				stream >> extension;
 				if (!stream.ok()
 					|| extension.size()
 						> details::kMaximumProxySettingsExtensionBytes) {
-					return false;
-				}
-				if (version == kProxySettingsExtensionVersion) {
+					invalidExtension = true;
+				} else if (version == kProxySettingsExtensionVersion) {
 					auto extensionStream = Serialize::ByteArrayReader(extension);
 					extensionStream
 						>> proxyCHtype
 						>> proxySlowMode
 						>> proxySlowDelay
 						>> proxySlowJitter
 						>> proxyPreferIPv6;
 					if (!extensionStream.ok() || !extensionStream.atEnd()) {
-						return false;
+						invalidExtension = true;
 					}
+				} else {
+					LOG(("App Info: "
+						"Future proxy settings extension ignored."));
 				}
 				// Unknown future versions are safely skipped by payload length.
 			} else {
-				return false;
+				invalidExtension = true;
 			}
 		} else {
 			// Compatibility with the previous unversioned five-field tail.
 			proxyCHtype = marker;
-			if (!stream.atEnd()) stream >> proxySlowMode;
-			if (!stream.atEnd()) stream >> proxySlowDelay;
-			if (!stream.atEnd()) stream >> proxySlowJitter;
-			if (!stream.atEnd()) stream >> proxyPreferIPv6;
+			if (!stream.atEnd()) {
+				stream >> proxySlowMode;
+			}
+			if (!stream.atEnd()) {
+				stream >> proxySlowDelay;
+			}
+			if (!stream.atEnd()) {
+				stream >> proxySlowJitter;
+			}
+			if (!stream.atEnd()) {
+				stream >> proxyPreferIPv6;
+			}
+			invalidExtension = !stream.ok();
 		}
 	}
 	const auto extensionFields = details::ProxySettingsExtensionFields{
 		.clientHelloType = proxyCHtype,
 		.slowMode = proxySlowMode,
 		.slowDelay = proxySlowDelay,
 		.slowJitter = proxySlowJitter,
 		.preferIPv6 = proxyPreferIPv6,
 	};
-	if (!details::IsProxySettingsExtensionValid(extensionFields)) {
-		return false;
-	}
-
-	if (!stream.ok()) {
+	if (invalidExtension
+		|| !details::IsProxySettingsExtensionValid(extensionFields)) {
 		LOG(("App Error: "
-			"Bad data for Core::SettingsProxy::setFromSerialized()"));
-		return false;
+			"Invalid proxy settings extension ignored."));
+		resetExtensionFields();
 	}
 
 	_tryIPv6 = (tryIPv6 == 1);
 	_useProxyForCalls = (useProxyForCalls == 1);
 	_checkIpWarningShown = (checkIpWarningShown == 1);
 	_proxyRotationEnabled = (proxyRotationEnabled == 1);
 	setProxyRotationTimeout(proxyRotationTimeout);
 	_settings = IntToProxySettings(settings);
diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_chromium_resumption_cache.h b/Telegram/SourceFiles/mtproto/details/mtproto_chromium_resumption_cache.h
--- a/Telegram/SourceFiles/mtproto/details/mtproto_chromium_resumption_cache.h
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_chromium_resumption_cache.h
@@ -52,17 +52,17 @@ public:
 			_entries.erase(i);
 			return { true, Lifetime(0) };
 		}
 		const auto expiry = *std::max_element(
 			begin(i->second),
 			end(i->second));
 		return {
 			true,
-			std::chrono::duration_cast<Lifetime>(expiry - now),
+			std::chrono::ceil<Lifetime>(expiry - now),
 		};
 	}
 
 	template <typename LifetimeGenerator>
 	[[nodiscard]] Lifetime confirm(
 			const Key &key,
 			LifetimeGenerator &&generateLifetime,
 			TimePoint now = Clock::now()) {
@@ -109,22 +109,26 @@ private:
 					end(slots),
 					[=](TimePoint expiry) { return expiry <= now; }),
 				end(slots));
 			i = slots.empty() ? _entries.erase(i) : std::next(i);
 		}
 	}
 
 	void eraseEarliestExpiring() {
+		const auto maximumExpiry = [](const Slots &slots) {
+			return slots.empty()
+				? TimePoint::min()
+				: *std::max_element(begin(slots), end(slots));
+		};
 		const auto oldest = std::min_element(
 			begin(_entries),
 			end(_entries),
-			[](const auto &a, const auto &b) {
-				return *std::max_element(begin(a.second), end(a.second))
-					< *std::max_element(begin(b.second), end(b.second));
+			[&](const auto &a, const auto &b) {
+				return maximumExpiry(a.second) < maximumExpiry(b.second);
 			});
 		if (oldest != end(_entries)) {
 			_entries.erase(oldest);
 		}
 	}
 
 	const std::size_t _maximumEntries;
 	const int _resumptionSlots;
