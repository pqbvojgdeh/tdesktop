diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_slow_connect_policy.h b/Telegram/SourceFiles/mtproto/details/mtproto_slow_connect_policy.h
--- a/Telegram/SourceFiles/mtproto/details/mtproto_slow_connect_policy.h
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_slow_connect_policy.h
@@ -12,7 +12,10 @@
 namespace MTP::details {
 
 inline constexpr auto kSlowConnectMaximumAdditionalDelayMs = 7500;
 inline constexpr auto kSlowConnectMaximumPending = 16;
+inline constexpr auto kSlowConnectMaximumSlots = 128;
+inline constexpr auto kSlowConnectOverflowMinimumDelayMs = 100;
+inline constexpr auto kSlowConnectOverflowMaximumDelayMs = 300;
 
 struct SlowConnectPolicy final {
 	int delay = 0;
@@ -41,4 +45,16 @@ struct SlowConnectPolicy final {
 	return std::chrono::milliseconds(policy.delay + policy.jitter);
 }
 
+[[nodiscard]] constexpr int NormalizeSlowConnectOverflowDelay(int value) {
+	return std::clamp(
+		value,
+		kSlowConnectOverflowMinimumDelayMs,
+		kSlowConnectOverflowMaximumDelayMs);
+}
+
+static_assert(kSlowConnectOverflowMinimumDelayMs > 0);
+static_assert(
+	kSlowConnectOverflowMinimumDelayMs
+		<= kSlowConnectOverflowMaximumDelayMs);
+
 } // namespace MTP::details
diff --git a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
--- a/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
+++ b/Telegram/SourceFiles/mtproto/details/mtproto_tls_socket.cpp
@@ -115,15 +115,38 @@ struct SlowConnectSchedule {
 	auto result = SlowConnectSchedule();
 	const auto policy = NormalizeSlowConnectPolicy(delay, jitter);
 	delay = policy.delay;
 	jitter = policy.jitter;
+	const auto overflowDelay = [] {
+		return sc::milliseconds(QRandomGenerator::global()->bounded(
+			kSlowConnectOverflowMinimumDelayMs,
+			kSlowConnectOverflowMaximumDelayMs + 1));
+	};
 	const QMutexLocker lock(&SlowConnectMutex());
 	auto &slots = SlowConnectSlots();
 	const auto now = sc::steady_clock::now();
 	for (auto i = slots.begin(); i != slots.end();) {
 		i = (i->second.pending == 0 && i->second.next <= now)
 			? slots.erase(i)
 			: std::next(i);
 	}
+	if (!slots.contains(endpoint)
+		&& slots.size() >= kSlowConnectMaximumSlots) {
+		const auto removable = std::min_element(
+			begin(slots),
+			end(slots),
+			[](const auto &a, const auto &b) {
+				if (a.second.pending != b.second.pending) {
+					return a.second.pending < b.second.pending;
+				}
+				return a.second.next < b.second.next;
+			});
+		if (removable == end(slots) || removable->second.pending != 0) {
+			result.overflow = true;
+			result.waitFor = overflowDelay();
+			return result;
+		}
+		slots.erase(removable);
+	}
 	const auto extra = jitter
 		? QRandomGenerator::global()->bounded(jitter + 1)
 		: 0;
@@ -141,12 +163,15 @@ struct SlowConnectSchedule {
 	result.overflow = (slot.pending >= kMaximumPendingDelayedConnects)
 		|| (next > maximum);
 	if (!result.overflow) {
 		slot.next = next;
 		if (result.waitFor.count() > 0) {
 			++slot.pending;
 			result.reservation = std::make_shared<SlowConnectReservation>(
 				endpoint);
 		}
+	} else {
+		// Do not turn local scheduler pressure into a simultaneous TCP burst.
+		result.waitFor = overflowDelay();
 	}
 	return result;
 }
@@ -2089,9 +2114,15 @@ void TlsSocket::connectToHost(const QString &address, int port) {
 		const auto endpoint = SlowConnectEndpoint(address, port);
 		const auto schedule = ReserveSlowConnect(endpoint, delay, jitter);
 		if (schedule.overflow) {
-			// Local queue pressure is not a network failure. Connect now instead
-			// of reporting an error that could quarantine a healthy proxy IP.
-			_socket.connectToHost(address, port);
+			// Local queue pressure is not a network failure, but connecting
+			// immediately would recreate the burst this scheduler prevents.
+			const auto _address = address;
+			const auto _port = port;
+			QTimer::singleShot(int(schedule.waitFor.count()), this, [=] {
+				if (_state == State::Connecting) {
+					_socket.connectToHost(_address, _port);
+				}
+			});
 			return;
 		}
 		if (schedule.waitFor.count() > 0) {
